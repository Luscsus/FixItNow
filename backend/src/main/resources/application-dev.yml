spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.example.backend: DEBUG
    org.springframework.security: DEBUG

app:
  frontend-url: http://localhost:5173

springdoc:
  swagger-ui:
    enabled: true
